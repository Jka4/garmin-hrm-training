{
	"name": "garmin-hrm-training",
	"version": "0.1.0",
	"private": true,
	"homepage": "https://jka4.github.io/garmin-hrm-training",
	"dependencies": {
		"gh-pages": "^3.1.0",
		"react": "17.0.1",
		"react-dom": "17.0.1",
		"react-scripts": "4.0.3"
	},
	"scripts": {
		"start": "PORT=1234 react-scripts start",
		"build": "react-scripts build",
		"deploy": "npm i && npm run build && gh-pages -d build"
	},
	"eslintConfig": {
		"extends": [
			"react-app"
		]
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	}
}
